{
    "version": "0.2.0",
    "configurations": [
      {
        "request": "launch",
        "type": "playdate",
        "name": "Run app in Playdate simulator",
        "source": "${workspaceRoot}/Source",
        "output": "${workspaceRoot}/output.pdx",
        "sdkPath": "${env:HOME}/Developer/PlaydateSDK"
      }
    ]
  }
